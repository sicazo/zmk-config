# Extra debug config for dongle-only local testing without Prospector hardware.
# Enables USB CDC ACM logging so host-side tools (fake_screen.py) can parse runtime state.

CONFIG_ZMK_USB_LOGGING=y
CONFIG_ZMK_LOG_LEVEL_DBG=y
